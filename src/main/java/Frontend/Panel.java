package Frontend;

import javax.swing.*;
import java.awt.*;

public class Panel extends JPanel {
    Panel(){
        this.setPreferredSize(new Dimension(100,200));
        this.setBackground(new Color(135, 142, 118));
    }
}
